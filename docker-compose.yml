services:
  limen:
    build: .
    ports:
      - "8452:8452"
    volumes:
      - limen-data:/data
    environment:
      - LIMEN_TOKEN=${LIMEN_TOKEN:-}
    restart: unless-stopped

volumes:
  limen-data:
